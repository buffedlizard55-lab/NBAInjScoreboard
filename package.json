{
  "name": "nba-live-injury-scoreboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "operator": "node tools/operator.mjs",
    "check": "for f in server.mjs assets/app.js src/*.mjs tools/*.mjs; do node --check \"$f\" || exit 1; done && node --test"
  },
  "devDependencies": {
    "linkedom": "^0.18.12"
  }
}
