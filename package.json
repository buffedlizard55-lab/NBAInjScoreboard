{
  "name": "nba-live-injury-scoreboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check assets/app.js && node --test"
  },
  "devDependencies": {
    "linkedom": "^0.18.12"
  }
}
